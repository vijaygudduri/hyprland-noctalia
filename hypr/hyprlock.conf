# sample hyprlock.conf
# for more configuration options, refer https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock
#
# rendered text in all widgets supports pango markup (e.g. <b> or <i> tags)
# ref. https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock/#general-remarks
#
# shortcuts to clear password buffer: ESC, Ctrl+U, Ctrl+Backspace
#
# you can get started by copying this config to ~/.config/hypr/hyprlock.conf
#

$font = Monospace

general {
    hide_cursor = false
}

# uncomment to enable fingerprint authentication
# auth {
#     fingerprint {
#         enabled = true
#         ready_message = Scan fingerprint to unlock
#         present_message = Scanning...
#         retry_delay = 250 # in milliseconds
#     }
# }

animations {
    enabled = true
    bezier = linear, 1, 1, 0, 0
    animation = fadeIn, 0, 0, linear
    animation = fadeOut, 1, 5, linear
    animation = inputFieldDots, 1, 3, linear
}

background {
    monitor =
    path = ~/Pictures/Wallpapers/Dark_Nature.png
    blur_passes = 2
}

input-field {
    monitor =
    size = 15%, 5%
    outline_thickness = 3
    inner_color = rgba(0, 0, 0, 0.0) # no fill

    outer_color = rgba(33ccffee) rgba(00ff99ee) 45deg
    check_color = rgba(00ff99ee) rgba(ff6633ee) 120deg
    fail_color = rgba(ff6633ee) rgba(ff0066ee) 40deg

    # font_color = rgb(143, 143, 143)
    font_color = rgba(196, 194, 194, 1)
    fade_on_empty = false
    rounding = -1

    font_family = $font
    # placeholder_text = Input password...
    # fail_text = $PAMFAIL
    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i> # can be set to empty

    # uncomment to use a letter instead of a dot to indicate the typed password
    # dots_text_format = *
    # dots_size = 0.4
    dots_spacing = 0.3

    # uncomment to use an input indicator that does not show the password length (similar to swaylock's input indicator)
    # hide_input = true

    placeholder_text = <span > $USER</span>
    hide_input = false
    position = 0, 350
    halign = center
    valign = bottom
}

image {
    monitor =
    path = ~/Pictures/Wallpapers/avtar.png
    shadow_size = 30
    shadow_color = rgb(0,0,0)
    size = 300
    rounding = 0 # no rounding
    border_size = 0
    position = 0, 420
    halign = center
    valign = bottom
}

# TIME
label {
    monitor =
    text = $TIME # ref. https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock/#variable-substitution
    font_size = 90
    font_family = $font

    position = -30, 0
    halign = right
    valign = top
}

# DATE
label {
    monitor =
    text = cmd[update:60000] date +"%A, %d %B %Y" # update every 60 seconds
    font_size = 25
    font_family = $font

    position = -30, -150
    halign = right
    valign = top
}

# Battery Info
label {
    monitor =
    text = cmd[update:5000] echo -e "$(~/.config/scripts/hyprlock-battery-info.sh)"
    font_size = 20
    font_family = JetBrains Mono Nerd Font Mono
    position = -30, -220
    halign = right
    valign = top
}

# sleep
label {
    monitor =
    font_family = JetBrains Mono Nerd Font Mono
    font_size = 24
    color = rgba(200,200,200,0.9)
    text = <span>󰖔</span> <span size="12000">Suspend</span>
    onclick = systemctl suspend
    position = 20, 30
    halign = left
    valign = bottom
}

# # logout
# label {
#     monitor =
#     font_family = JetBrains Mono Nerd Font Mono
#     font_size = 24
#     text = <span>󰿅 </span>
#     onclick = hyprctl dispatch exit
#     position = 60, 30
#     halign = left
#     valign = bottom
# }

# # reboot
# label {
#     monitor =
#     font_family = JetBrains Mono Nerd Font Mono
#     font_size = 24
#     text = <span>󰜉 </span>
#     onclick = systemctl reboot
#     position = 100, 30
#     halign = left
#     valign = bottom
# }

# # poweroff
# label {
#     monitor =
#     font_family = JetBrains Mono Nerd Font Mono
#     font_size = 24
#     text = <span>󰐥 </span>
#     onclick = systemctl poweroff
#     position = 140, 30
#     halign = left
#     valign = bottom
# }
